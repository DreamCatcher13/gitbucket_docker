apiVersion: apps/v1
kind: Deployment
metadata:
  name: psql-nfs-server
  namespace: app-inf
  labels:
    app: nfs
    server: psql-nfs
spec:
  replicas: 1
  selector:
    matchLabels:
      server: psql-nfs
  template:
    metadata:
      labels:
        server: psql-nfs
    spec:
      containers:
      - name: psql-nfs-server
        image: gcr.io/google_containers/volume-nfs:0.8
        ports:
        - name: nfs
          containerPort: 2049
        - name: mountd
          containerPort: 20048
        - name: rpcbind
          containerPort: 111
        securityContext:
          privileged: true
        volumeMounts:
        - mountPath: "/exports"
          name: psql-m
      volumes:
      - name: psql-m
        gcePersistentDisk:           
          pdName: psql-nfs
          fsType: ext4
        
---
apiVersion: v1
kind: Service
metadata:
  name: psql-nfs-service
  namespace: app-inf
spec:
  selector:
    server: psql-nfs
  ports:
  - name: nfs
    port: 2049
    targetPort: 2049
  - name: mountd
    port: 20048
    targetPort: 20048
  - name: rpcbind
    port: 111
    targetPort: 111

